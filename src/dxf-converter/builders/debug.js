export default class DebugBuilder {
    constructor(type) {
        console.log(`${type} being processed by debug builder`); // eslint-disable-line
    }
    addGroup() {
        // no op
    }

    getResult() {
        // no op
    }
}
